program --> program decl_var EOL
program --> program decl_var EOL
program --> COMMT EOL 

    decl_var --> VAR IDS DSPTS tipo EQ exp TERMINAL
    decl_var --> VAR IDS DSPTS tipo 

    decl_func --> FN IDS (lista) DSPTS tipo {def} TERMINAL

        tipo --> INT
        tipo --> FLOAT
        tipo --> BOOL

        exp --> L_INT
        exp --> L_FLOAT
        exp --> TRUE 
        exp --> FALSE
        exp --> calc
        
        lista --> VAZIO
        lista --> IDS tipo
        lista --> IDS tipo VIRG lista

        def --> decl_var
        def --> attr
        def --> cond
        def --> laco
        def --> ret

            calc -->

            attr --> IDS EQ exp

            condif --> IF (cond) {def} ELSE {def} TERMINAL 
            condif --> IF (cond) {def} TERMINAL

            laco --> WHILE (cond) {def} TERMINAL

            ret --> RETURN exp TERMINAL

                cond --> IDS tipo VIRG lista
                cond -->
                cond -->
                cond -->

